[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && python manage.py migrate && daphne tutlee.asgi:application --port $PORT --bind 0.0.0.0"
